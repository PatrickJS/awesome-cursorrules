- Use Expo Router for navigation - never set up React Navigation manually in Expo projects
- Use EAS Build for native builds - never use deprecated expo build
- Use expo-image for images - never use React Native's built-in Image component
- Use expo-secure-store for sensitive data - never use AsyncStorage for tokens or secrets
- Use Expo config plugins for native configuration - never edit native iOS/Android files directly
